export interface ShortPortfolioInterface {
  id: number;
  title: string;
  total: number;
  image: string; // You can change the type if it's not a string
  userID: number; // You can change the type if it's not a number
  createdAt: string; // You can change the type if it's not a string
  updatedAt: string; // You can change the type if it's not a string
}
